<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jd.wms.dao.mapper.TaskMapper">

    <select id="selectByRelatedNo" resultType="com.jd.wms.dao.entity.Task">
        SELECT * FROM task WHERE related_no = #{relatedNo}
    </select>

</mapper>